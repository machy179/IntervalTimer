<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NumberPickerStyle">
        <item name="android:textColorPrimary">@color/colorPisma</item>
        <item name="android:textColor">@color/colorPisma</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/velikostTextuHodnoty</item>
        <item name="colorControlNormal">@color/colorPisma</item>


        <item name="color">@color/colorPisma</item>
    </style>

    <style name="DialogStyle">
        <item name="android:textColor">@color/colorPisma</item>
        <item name="android:textStyle">bold</item>
        <item name="android:labelTextSize">@dimen/velikostTextuNadpisDialog</item>
        <item name="android:keyTextSize">@dimen/velikostTextuHodnoty</item>
        <item name="android:divider">@color/colorPisma</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
